<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Links</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"/>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">

</head>
<body>
    <div class="container">
        <h1>Links</h1>
        <a href="/add">Adicionar Links</a>
        <%for(let i = 0; i < links.length; i++){%>
            <div class="card p-3 mb-3" id="<%=links[i]._id%>">
                <form class="d-flex justify-content-between align-items-center" method="POST" action="/<%=links[i]._id%>?_method=DELETE">
                    <div class="d-flex flex-column">
                        <h2><%=links[i].title%></h2>
                        <div><%=links[i].description%></div>
                        <div class="text-secondary"><%=links[i].url%></div>
                        <div class="text-info"> <i class="material-icons">bar_chart</i> Clicks: <%=links[i].click%></div>
                    </div>
                    <div class="d-flex flex-column">
                        <button class="btn btn-danger mb-1" type="submit">Delete</button>
                        <a class="btn btn-info" href="/edit/<%=links[i]._id%>">Edit</a>
                    </div>
                </form>
            </div>
        <%}%>
    </div>

</body>
</html>