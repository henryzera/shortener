<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Encurtador</title>
</head>
<body>
    <h1>Edit Link</h1>

    <form action="/edit/<%=body._id%>" method="POST">
        <input class="field" name="title" placeholder="Title" type="text" value="<%=body.title%>">
        <input class="field" name="description" placeholder="Description" type="text" value="<%=body.description%>">
        <input class="field" name="url" placeholder="URL" type="text" value="<%=body.url%>">
        <button>Save</button>

        <%if(error){%>
            <h3><%=error.message%></h3>
        <%}%>
    </form>
</body>
</html>